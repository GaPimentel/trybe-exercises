//1 - "Olá xyz"
/* let names = {
  person1: 'João',
  person2: 'Maria',
  person3: 'Jorge',
};
function hello (object) {
  for (const key in object) {
    const element = object[key];
    console.log("Olá " + element);
  };
}
hello (names); */

//2 - chaves e valores:
/* let car = {
  model: 'A3 Sedan',
  manufacturer: 'Audi',
  year: 2020
};
function carDescription (object) {
  for (const key in object) {
    const element = object[key];
    console.log(key, element);
  };
}
carDescription (car); */
